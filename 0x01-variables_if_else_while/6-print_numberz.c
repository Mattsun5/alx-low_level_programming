#include <stdio.h>
/**
 * main - entry point
 * Return - 0 when successful
 */
int main(void)
{
	char num;

	num = 0;
	while (num < 10)
	{
		putchar(int num);
		num++;
	}
	printf("\n");
	return (0);
}
