#include "main.h"
/**
 * _abs - absolute value of an integer
 * @n: interger to compute
 * Return: absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
